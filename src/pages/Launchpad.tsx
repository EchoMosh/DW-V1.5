/**
 * Dashboard page (Launchpad)
 * Premium glassmorphic bento dashboard with charts.
 */
import PremiumDashboard from "@/components/PremiumDashboard";

const Launchpad = () => {
  return (
    <>
      <PremiumDashboard />
    </>
  );
};

export default Launchpad;
